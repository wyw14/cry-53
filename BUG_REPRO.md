# Bug Reproduction

Validate two configurations in different environments that reference each other. The result contains neither the environment conflict nor the dependency cycle.

Both omissions occur deterministically for this fixed graph.
